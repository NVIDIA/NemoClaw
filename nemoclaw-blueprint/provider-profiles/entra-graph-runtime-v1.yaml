# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

# Template profile for the experimental blueprint identity wrapper. Copy this
# file and replace `common` with the Entra tenant ID or domain approved for the
# workload before use. Keep the Graph scope and endpoint least-privileged.
id: entra-graph-runtime-v1
display_name: Microsoft Entra Graph Runtime Credentials v1
description: Gateway-managed Microsoft Graph access-token refresh for an attached sandbox
category: agent
credentials:
  - name: MS_GRAPH_ACCESS_TOKEN
    description: Short-lived delegated Microsoft Graph access token
    env_vars:
      - MS_GRAPH_ACCESS_TOKEN
    required: true
    auth_style: bearer
    header_name: authorization
    refresh:
      strategy: oauth2_refresh_token
      token_url: https://login.microsoftonline.com/common/oauth2/v2.0/token
      scopes:
        - https://graph.microsoft.com/User.Read
      refresh_before_seconds: 300
      max_lifetime_seconds: 3600
      material:
        - name: client_id
          required: true
        - name: refresh_token
          required: true
          secret: true
        - name: client_secret
          required: false
          secret: true
endpoints:
  - host: graph.microsoft.com
    port: 443
    protocol: rest
    enforcement: enforce
    rules:
      - allow: { method: GET, path: "/v1.0/me" }
binaries:
  - /usr/local/bin/node
  - /usr/bin/node
  - /usr/local/bin/curl
  - /usr/bin/curl
inference_capable: false
