diff --git a/deepagents_code/offload.py b/deepagents_code/offload.py
index a42e02e..94a4380 100644
--- a/deepagents_code/offload.py
+++ b/deepagents_code/offload.py
@@ -103,0 +104,4 @@ def _harden_dir(path: Path) -> None:
+    # The native launcher protects the share DACL before MXC adds its grant.
+    # Keep that inherited grant; POSIX chmod can fail inside the approved root.
+    if os.name == "nt":
+        return
